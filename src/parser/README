% wget http://mysql.mirrors.pair.com/Downloads/MySQL-5.5/mysql-5.5.14.tar.gz
% tar zxf mysql-5.5.14.tar.gz
% cd mysql-5.5.14
% mkdir build
% cd build
% cmake -DWITH_EMBEDDED_SERVER=ON ..
% make

now, copy config.mk.sample to config.mk and adjust the path for MYSRC
may need to sudo apt-get install libbsd-dev 

---

Some notes about Item processing:
    Item::transform, Item::compile, and Item::traverse_cond could be useful

